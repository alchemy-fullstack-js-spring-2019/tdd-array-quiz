function wordArray(array) {
    array.filter(fn() {
      if(array.filter(!array.contains('y')) {

      }
    }
      this.arr.includes

    this.arr.filter(fn(n) => this.arr.includes(this.array[i]);
    console.log(array);
    return array;
  }
  
  function step2() {
    step1.map();

  }
  step1();
  step2();
};
// I don't know if I should make a class or a function, and this hasn't passed travis from step 1 (after @ 
// 15 minutes I had to stop caring and start the actual test.)
module.exports = { wordArray };
