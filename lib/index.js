function removeY(arr) {
    const removed = {};
    const searchTerms = ['Y', 'y'];
    for (let i = arr.length-1; i >=0; i++) {
        if (arr[i].includes('Y'||'y'))
    }
}

module.exports = { removeY };