const { isItWithNoY } = require('../lib/index');

describe('Makes an is it function with no Ys upper or lowercase', () => {
  it('checks the array for Ys both upper and lowercase', () => {
    const array = ['sky', 'hot', 'Yeti', 'green']	

    expect()
  });
};
