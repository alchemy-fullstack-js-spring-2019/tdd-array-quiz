


describe('function that creates a new array and returns an array that removes word beginning with y', () => {
  it('can create a new array', () => {
    const arr = ['yellow', 'blue', 'green']
    const newArr = arr.map();
    return newArr;
  });
});
