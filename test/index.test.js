const { dontAskWhy } = require('../lib/index');

describe('TDD Testing', () => {
  it('removes words and returns a new array', () => {
    const initArray = ['sky', 'hot', 'Yeti', 'green'];
    const newArray = ['Is it hot?', 'Is it green?'];

  });
});
