const { filterY } = require('../lib/testFunction.js');

describe('filter function test', () => {

  it('return a new array that filters out the letter y', () => {
    const sampleData = ['yeti', 'hot', 'sky', 'butterfingers']; 

  });
    
});
